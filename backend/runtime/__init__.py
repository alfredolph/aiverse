"""本地推理运行时 —— 一键部署 H3 生产环境。

文档 §19-§22 / §35 / §36 的落地：
用户装完 AIVerse 后不需要自己折腾 Python / CUDA / ComfyUI / 模型权重，
点一次「一键部署」，程序自动完成全部环境准备，之后直接用本地显卡出片。

模块：
    catalog.py     组件清单（版本、体积、来源、镜像）
    planner.py     按硬件生成部署计划
    downloader.py  断点续传下载器（多镜像、进度、限速）
    installer.py   步骤执行引擎（可暂停/取消/重试，状态持久化）
    manager.py     对外门面
"""
